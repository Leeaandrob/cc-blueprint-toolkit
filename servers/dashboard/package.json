{
  "name": "@bp/dashboard-server",
  "version": "1.0.0",
  "description": "MCP Dashboard Server for BP Autonomous Loop monitoring",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "dev": "tsc && node build/index.js",
    "clean": "rm -rf build",
    "prepublish": "npm run build"
  },
  "keywords": [
    "mcp",
    "dashboard",
    "monitoring",
    "autonomous",
    "tdd"
  ],
  "author": "Leeaandrob",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
